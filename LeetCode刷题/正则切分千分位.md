```javascript
let str = "12345678.1212";

function splitByTh(str) {
  return str.replace(/(\d)(?=(\d{3})+$)/g, "$1,");
}

function splitByTh(str) {
  return str.replace(/(\d)(?=(\d{3})+\.)/g, "$1,");
}
```

?= 是不记录匹配的
